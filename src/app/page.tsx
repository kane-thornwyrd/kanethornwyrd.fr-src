import styles from "./page.module.css";

export default function Home() {
  return (
    <main>
      {/* <div className="flex flex-col xl:flex-row">
        <section className="backdrop-blur-xl shadow-xl rounded-lg md:m-2">
          <h2>Projects</h2>
          <p className="prose lg:prose-xl mx-2">
            You can also use variant modifiers to target media queries like
            responsive breakpoints, dark mode, prefers-reduced-motion, and more.
            For example, use <code>md:subpixel-antialiased</code> to apply the
            subpixel-antialiased utility at only{" "}
            <strong>medium screen sizes</strong> and above.
          </p>
        </section>
        <section className="backdrop-blur-xl shadow-xl rounded-lg md:m-2">
          <h2>Blog</h2>
          <p className="prose lg:prose-xl mx-2">
            You can also use variant modifiers to target media queries like
            responsive breakpoints, dark mode, prefers-reduced-motion, and more.
            For example, use <code>md:subpixel-antialiased</code> to apply the
            subpixel-antialiased <a href="#">Lien de test</a> utility at only{" "}
            <strong>medium screen sizes</strong> and above.
          </p>
        </section>
        <section className="backdrop-blur-xl shadow-xl rounded-lg md:m-2">
          <h2>Portfolio</h2>
          <p className="prose lg:prose-xl mx-2">
            You can also use variant modifiers to target media queries like
            responsive breakpoints, dark mode, prefers-reduced-motion, and more.
            For example, use <code>md:subpixel-antialiased</code> to apply the
            subpixel-antialiased utility at only{" "}
            <strong>medium screen sizes</strong> and above.
          </p>
        </section>
      </div>
      <section className="backdrop-blur-xl shadow-xl rounded-lg md:m-2">
        <h2>Contact</h2>
      </section> */}
    </main>
  );
}
